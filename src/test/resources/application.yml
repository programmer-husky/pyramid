server:
  port: 80
spring:
  redis:
    host: 127.0.0.1
    database: 0
    port: 6379
    lettuce:
      pool:
        max-wait: 1000
        min-idle: 20
        max-idle: 100
        max-active: 100
pyramid:
  enable: true
  distributed-enable: true
  node: programmer_husky
  local:
    enable: false
    cache-seconds: 1
